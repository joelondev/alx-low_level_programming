#include "main.h"
/**
 * main - a function that prints the alphabet in lowercase
 *
 * Return: 0
 */

void print_alphabet(void)
{
	char q;

	for (q = 'a'; q <= 'z'; q++)
		_putchar(q);
	_putchar('\n');
	
}
